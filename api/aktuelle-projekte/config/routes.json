{
  "routes": [
    {
      "method": "GET",
      "path": "/aktuelle-projekte",
      "handler": "aktuelle-projekte.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/aktuelle-projekte/count",
      "handler": "aktuelle-projekte.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/aktuelle-projekte/:id",
      "handler": "aktuelle-projekte.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/aktuelle-projekte",
      "handler": "aktuelle-projekte.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/aktuelle-projekte/:id",
      "handler": "aktuelle-projekte.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/aktuelle-projekte/:id",
      "handler": "aktuelle-projekte.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
